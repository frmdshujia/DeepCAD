# DeepCAD 项目依赖
# 
# 注意：此文件包含基础依赖，完整安装请使用 scripts/install_environment.sh
# 或参考 docs/ENVIRONMENT_SETUP.md 进行分步安装
#
# 版本选择策略：
# - Python 3.10（兼容 RETFound/MedSAM/MMCL）
# - PyTorch 2.0.1（兼容 MedSAM 和 RETFound）
# - numpy 1.26.4（兼容 RETFound，MedSAM 的 numpy>=2.0.0 要求可能过于严格）

# 基础依赖
# PyTorch 需要根据 CUDA 版本单独安装，请参考安装脚本
# torch>=2.0.0,<2.1.0
# torchvision>=0.15.0,<0.16.0

# numpy（固定版本以避免与 RETFound/MedSAM 冲突）
numpy==1.26.4

# 图像处理
pillow>=9.0.0,<11.0.0  # RETFound 需要 ~10.2.0

# 数据处理
pandas>=1.3.0
scipy>=1.7.0

# 配置管理
pyyaml>=6.0
omegaconf>=2.2.0

# 可视化
matplotlib>=3.5.0
seaborn>=0.11.0

# 日志和实验跟踪
tensorboard>=2.10.0
wandb>=0.12.0  # 可选

# 医学图像处理
nibabel>=3.2.0  # 用于处理 NIfTI 格式的 MRI 数据
scikit-image>=0.19.0
SimpleITK>=2.2.1  # MedSAM 需要

# RETFound 相关依赖
timm==0.9.2  # RETFound 需要特定版本
opencv-python==4.9.0.80  # RETFound 需要特定版本
huggingface-hub>=0.23.4  # 用于下载 RETFound 权重

# 其他工具
tqdm>=4.64.0
scikit-learn>=1.0.0  # 可选，用于分析和可视化

